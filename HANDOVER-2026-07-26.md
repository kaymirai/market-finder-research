# 引継書 2026-07-26

対象: Market Finder / Chrome拡張 / 共有キーワードエンジン
ブランチ: `codex/market-finder-research-improvements`（`main` より34コミット先行、遅れなし）
最終コミット: `0286eab`

---

## 1. 最初に読むべき3行

- **作業ディレクトリは `C:\work\AntiGravity\Etsy_product`（リポジトリ本体）です。** `.worktrees/final-keyword-guidance` にも古いチェックアウトが残っていますが、Chromeが読み込む拡張は本体側なので、こちらでは作業しないでください。
- 未コミットの変更はありません。`.claude/` `.superpowers/` `AGENTS.md` `CLAUDE.md` `mockup-batch-studio/` は未追跡のまま意図的に残しています。
- テストは `market-finder/scripts/test-*.mjs` の13ファイル248件が全通過、`validate-scoring.mjs` もPASSの状態で引き渡します。

---

## 2. 今回のセッションで入れたもの

コミット単位で、**何をしたか**より**なぜそうしたか**を残します。ここを踏まえずに戻すと同じ問題が再発します。

### `16e5bba` 買い手基点の候補生成（8軸）

候補生成の入口がカテゴリとイベントだけだったため、`fathers day shirt` には届いても `pediatric speech therapist shirt` には原理的に届きませんでした。買い手の属性を軸として掛け合わせる純粋関数を `shared/market-keyword-engine/index.js` に追加しています。

### `5a157fa` Etsy公式を先、eRankを後ろへ（**設計変更**）

**元に戻さないでください。** 以前はEtsy公式の候補を `buildEtsyCandidatesFromErank()` からしか作れず、eRankが毎回クリティカルパス上にありました。しかしeRankは1日100回の上限があり、実測では33語中2語しか判定を変えていません。一方 Marketplace Insights は課金により無制限です。

- `etsyValidationCandidates()` は eRank行が無ければ `buildEtsyCandidatesFromPool()` にフォールバックします。
- ステージ順は 条件 → 候補 → **Etsy公式** → **eRank** → 最終結果 です（`RESEARCH_STAGE_IDS` in `market-finder/src/research-console-ui.js`）。
- **eRankは省略可能**です。Etsy公式で数字が取れていればEverBeeへ直行できます。

### `29f7820` 買い手語彙のカタログ（240語）

8軸のうち Identity / Occupation / Hobby / Emotion-context は掛け合わせの型では作れない「語彙」です。空欄で渡していたのが最大のボトルネックでした。`BUYER_IDENTITY_LIBRARY` に10グループ240語を持ち、`suggestBuyerIdentities()` がグループを**ラウンドロビンで1つずつ**拾います。

**12件を1グループから出さないでください。** 選ぶ基準は検索数ではなく「自分が内容を分かるか」であり（戦略v2「最低3本は自分が理解している領域」）、1つの世界の語しか画面に出ないとその判断自体が不可能になります。`nurse` `teacher` `mom` のような広い語はテストで混入を禁止しています。

### `5e14724` 贈る人の明示と名入れレバー

Etsyでは検索者と着用者が別人であることが多いのに、全フレーズが着用者視点でした。`classifyBuyerIdentity()` が語の種別（occupation / identity / hobby）を判定し、種別ごとに文法の通る言い回しだけを適用します。

`for my grandma` は成立し `for my nicu nurse` は成立しない、`nurse appreciation` は成立し `dog mom appreciation` は成立しない、という区別のためです。全種別に同じ型を当てると候補枠を誰も打たない語で消費します。

### `043eed9` 修飾語の実測（**この方針が中核**）

**ここが今回いちばん重要な変更です。** それまでの修飾語リストは全て開発者の推測でした。`validation/` の実測データ（Etsy検索語27件・売れている商品26件）で数えたところ、こうなりました。

| 語 | 需要 | 供給 |
|---|---:|---:|
| `from` | 0 | 0 |
| `gift` | 0 | 0 |
| `appreciation` | 0 | 0 |
| `personalized` | 1語/7検索 | 2件/月12販 |

`analyzeModifierUsage()` が需要（Etsy関連キーワード）と供給（EverBeeの売れている商品タイトル）を**別々に**数えます。両方が使う語は既に取られた市場、需要だけある語が空いている入口です。

実装上の落とし穴が1つあります。**`GENERIC_WORDS` を除外語に流用しないでください。** そこには `gift` `personalized` `from` が入っており、測りたい語が測定前に消えます。専用の `MODIFIER_STOP_WORDS`（商品名と文法語のみ）に分けてあります。

**リスク判定は測定の後に必ず通してください。** 実測1位は `summerween`（差+42.3）でしたが商標リスク語です。検索数が多いことこそ手を出す理由になるため、`measuredModifierPhrases()` 内で `detectRiskTerms()` を通しています。

### `0286eab` 調査結果のファイル保管

実測は蓄積するほど精度が上がるのに、データはブラウザのlocalStorageにしかありませんでした。静的サーバに追記専用の保管エンドポイントを追加しています。

- **ファイル名はサーバ側生成です。** ブラウザから受け取ったパスは使いません（トラバーサル対策、テスト済み）。
- 合算時は語句＋数値で**重複排除**します。保管ファイルはメモリ上の同じ調査のスナップショットなので、素直に足すと二重計上され、何度も保存したニッチが実測を支配します。

**Supabaseにしなかった理由**（`market-finder/archive/README.md` に詳述）: 1人1台で使う限り失われる原因はブラウザのデータ消去であり、それはファイルで解決します。gitの差分で時系列比較もできます。**複数PCから見たい、または `etsy-product-ai` 本体から読みたくなった時点**が移行の判断点で、形式を1調査＝1レコードにしてあるので取り込みスクリプトだけで済みます。

---

## 3. 動かし方（ここで時間を溶かしました）

### 静的サーバ

```bash
node market-finder/scripts/static-server.mjs . 4174
```

`python -m http.server` を使わないでください。キャッシュヘッダが無いため古いJSが配信され続けます。またリポジトリ直下を配信すると `.env.local` が露出します。上記サーバは `Cache-Control: no-store` を返し、`/market-finder/` と `/shared/` だけを配信します。

保管機能はこのサーバ経由でのみ動きます。`file://` で開くとボタンが無効化され、理由が表示されます。

URL: `http://127.0.0.1:4174/market-finder/`

### キャッシュ版数（毎回必要）

`index.html` `src/app.js` `styles.css` `shared/market-keyword-engine/index.js` の `?v=` を**変更のたびに上げてください**。現在 `20260726-8`。上げ忘れると古いエンジンが配信され、原因不明の挙動になります（実際に1度踏みました）。

`test-guided-entry-ui.mjs` に版数一致のアサーションがあるので、そちらも同時に更新します。

ユーザは `Ctrl+Shift+R` が画面キャプチャに割り当たっているため使えません。**普通のF5で反映されることが要件**です。

### Chrome拡張

```bash
cd etsy-chrome-extension && npm run build
```

- `manifest.json` と `market-finder/src/app.js:118` の `REQUIRED_EXTENSION_VERSION` は**両方1.37**。片方だけ変えると接続が通りません。
- 拡張を変更したら**必ずバージョンを上げてください**。上げないと、読み込まれているのが新旧どちらのビルドか判別できません（1度これで無駄に調査しました）。
- Chromeが読み込むのは**リポジトリ本体**の `etsy-chrome-extension/` です。worktree側ではありません。

### テスト

```bash
node --test market-finder/scripts/test-opportunity-model.mjs
```

全件は各ファイルを個別に実行します（13ファイル248件）。加えて `node market-finder/scripts/validate-scoring.mjs` が回帰ケースを検証します。

**CRLFに注意してください。** テストが `\n` 前提の正規表現を持つため、ファイル全体がCRLFになると落ちます。編集後にLFへ正規化してください。

---

## 4. 触るときに壊しやすい箇所

| 箇所 | 注意 |
|---|---|
| `erankContent.ts` の `submitSearch` | Enter送信 → 1.2秒待機 → 開始判定 → **その後で**近傍ボタン探索、の順序が必須です。順序を変えると "Go to Dashboard" を押します（実害あり） |
| eRankのタイムアウト | 内側 `ERANK_METRICS_READY_TIMEOUT_MS = 300000`、外側 `ERANK_KEYWORD_TIMEOUT_MS = 330000`。**外側は内側+30秒以上**。テストが強制します |
| EverBeeの商品行取得 | MUI DataGridは列を仮想化するため、`.MuiDataGrid-virtualScroller` を400px刻みで走査しながら収集しないとreviews等が取れません |
| 非アクティブタブ | Chromeがタイマーを絞るため、`document.visibilityState` で隠れていた時間を除外して計測しています。単純な経過時間に戻さないでください |
| `analyzeModifierUsage` の除外語 | `GENERIC_WORDS` を使わない（前述） |

---

## 5. 残作業

優先度順。行番号は `0286eab` 時点。

### 高

1. **MV3の状態復元** — `etsy-chrome-extension/src/background.ts:1331` で `chrome.storage.local.set({ marketState })` を書いていますが、対応する `get` がありません。Service Workerが停止するとメモリ上の `marketActive` 等が失われ、進行中の調査が宙に浮きます。`onStartup` / 起動時に復元する処理が必要です。
2. **複数形のIP語検出漏れ** — `swifties shirt` がリスク未検出です（`swiftie shirt` は検出）。`hobbits` `grinches` は検出できているので、`-ies` の正規化だけが漏れています。`detectRiskTerms` の複数形処理を確認してください。

### 中

3. **eRank残枠の表示** — 1日100回（Basic）/200回（Pro）の上限に対し、消費見込みは出しますが残枠は出していません。ユーザが「あと何語調べられるか」を判断できません。
4. **eRank関連キーワードの取得上限** — 上限が無く、大量取得時に時間がかかります。
5. **地名の扱い** — 現在 `reject` ですが、`review` の方が妥当です（地名ニッチは実在するため）。
6. **未来デザイナー側のCSVアップロード枠ガード** — `etsy-product-ai` 側。上限を超えた場合の挙動が未確認です。

### 低

7. **ドキュメントのポート/バージョン記載ずれ** — 一部のplans/specsに古い記載が残っています。`plans/` `specs/` `validation/` は当時の決定の記録なので、**現状に合わせて書き換えないでください**。更新対象は `README.md` と `SCREEN_GUIDE.md` だけです。
8. **名入れレバーの下流接続** — `personalizable` フラグはバッジ表示までです。戦略v2 §1の「勝った買い手意図には必ずスウェット版か名入れ版を作る」という運用ルールが、最終結果のCSVや商品展開の指示に繋がっていません。

---

## 6. 未検証の前提（引き継ぎ先で確認してほしいこと）

**修飾語の実測は現時点で1ニッチ・53サンプルしかありません。** Halloweenはイベント系のため、贈答語が母の日などより出にくい可能性が残ります。「`gift` は使われない」と結論づけるのは早計です。

数ニッチ分を保管した時点で、2「候補」の実測表を確認してください。組み込みの想定語（`gift` `from` `appreciation` 等）が本当に0のままなら、`BUYER_GIVER_PHRASES` と `BUYER_GIFT_SUFFIXES` を実測語に置き換える判断ができます。候補には `実測語` / `想定語` のバッジが出るので、どちらに立脚しているかは画面で判別できます。

---

## 7. 関連ドキュメント

| ファイル | 内容 |
|---|---|
| `market-finder/README.md` | 起動、拡張連携、保管、スコアリングの概要 |
| `market-finder/SCREEN_GUIDE.md` | 画面ごとの操作手順（8軸の使い方を含む） |
| `market-finder/archive/README.md` | 保管形式とSupabase移行の判断基準 |
| `market-finder/validation/` | 実測データ。**書き換えないでください**（測定の基準点です） |
| `C:\Users\kayso\Documents\Codex\2026-07-19\etsy-10-1-500-200-notebooklm\reports\2026-07-25-etsy-profit-strategy-v2.md` | 戦略v2。8軸の定義、月次目標、eRankの位置づけの根拠 |

---

## 8. ユーザから受けている指示

- 細かい確認は不要。致命的なエラーの可能性がない限り、逐一の許可を取らずに最後まで実行してよい。
- 実装後は必ずブラウザで動作確認する。テストが通っただけでは完了としない。
